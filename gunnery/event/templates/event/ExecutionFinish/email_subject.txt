[{{ instance.task.application.name }}] {% if instance.status == instance.SUCCESS %}SUCCESSFUL{% elif instance.status == execution.ABORTED %}ABORTED{% else %}FAILED{% endif %} execution of task {{ instance.task.name }} on {{ instance.environment.name }}