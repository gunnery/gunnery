language: python
python:
  - "2.7"
install:
  - pip install -r requirements/development.txt
script:
  - (cd gunnery; python manage.py test --settings=gunnery.settings.test -v2)
env:
  - SECRET_KEY=test DATABASE_DEFAULT_NAME=test DATABASE_DEFAULT_USER=test DATABASE_DEFAULT_PASSWORD=test DATABASE_DEFAULT_HOST=test
